README.md
pkg-list-aur.txt
pkg-list.txt

{{- $isLinux := eq .chezmoi.os "linux" }}
{{- $osReleaseID := "" }}
{{- $kernelRelease := "" }}
{{- if $isLinux }}
{{- $osReleaseID = get .chezmoi.osRelease "id" }}
{{- $kernelRelease = get .chezmoi.kernel "osrelease" }}
{{- end }}
{{- $isArch := and $isLinux (eq $osReleaseID "arch") }}
{{- $isWSL := and $isLinux (contains "microsoft" (lower $kernelRelease)) }}
{{- if not (and $isArch (not $isWSL)) }}
# Desktop-only config. Apply only on native Arch Linux, not WSL or non-Arch hosts.
.config/sunsetr/
.config/sunsetr/**
.config/hypr/
.config/hypr/**
.config/gtk-4.0/
.config/gtk-4.0/**
.config/kitty/
.config/kitty/**
.config/walker/
.config/walker/**
.config/waybar/
.config/waybar/**

{{- end }}
